## Golang Clean Architecture
Based on https://github.com/evrone/go-clean-template

### NOTES:
- Use ngrok to expose api publicly
- Can't use swagger on public url (ngrok), can access the swagger ui but not the api, use postman instead (import the yaml to postman)
- Can't assign public url using ngrok on midtrans payment notification url


